# Story 3.2: 제안서 팝업 & 비교 차트

Status: review

## Story

As a 관리자,
I want "제안서 출력" 버튼 클릭 시 도표와 차트가 포함된 1페이지 제안서 팝업을 확인하고 싶다,
So that 시연 자리에서 바로 보고서 품질의 제안서를 보여줄 수 있다.

## Acceptance Criteria

1. **Given** 결과 화면에서 "제안서 출력" 클릭 **When** ProposalModal 렌더링 **Then** 2초 이내에 680px 너비(A4 비율) 팝업이 열림. 브랜드 로고(텍스트), 물건 정보, 수치 도표, 비교 차트, 판정 배지, 출력 날짜가 모두 표시됨.

2. **Given** 유사 지점 대비 분석 대상의 월매출 데이터 **When** ComparisonBarChart 렌더링 **Then** Recharts 막대그래프에서 분석 대상 바가 `#2563EB` 파란색으로 강조되고 유사 지점 바는 `#BFDBFE`로 표시됨.

3. **Given** ProposalModal이 열린 상태 **When** X 버튼 클릭 **Then** 팝업이 닫힘. ESC 키 또는 배경 클릭으로는 닫히지 않음 (의도적 UX).

4. **Given** 팝업 내 수치 도표 **When** 렌더링 **Then** 예상 월매출, 점유율, 순수익이 각각 강조된 카드 형태로 표시됨.

## Tasks / Subtasks

- [x] Task 1: 백엔드 SimulationResult에 comparison_data 추가 (AC: #2)
  - [x] `fastapi_backend/app/schemas/simulation.py`에 `ComparisonEntry(BaseModel)` 추가 (name: str, monthly_revenue: float)
  - [x] `SimulationResult`에 `comparison_data: list[ComparisonEntry] = []` 필드 추가
  - [x] `fastapi_backend/app/engines/rule_based.py`에서 `effective` 목록 중 상위 5개를 `comparison_data`로 반환 (avg_monthly_revenue 내림차순, 이름 최대 12자 + "…" 트런케이트)
  - [x] `_default_estimate()`도 `comparison_data=[]` 반환 (기본값이라 변경 불필요)
  - [ ] 기존 시뮬레이션 테스트에 comparison_data 필드 존재 확인 assertion 추가

- [x] Task 2: 프론트엔드 패키지 설치 + Dialog UI 컴포넌트 (AC: #3)
  - [x] `cd nextjs-frontend && npm install recharts` (recharts v2에 TypeScript 타입 내장, @types 별도 불필요)
  - [x] `cd nextjs-frontend && npm install @radix-ui/react-dialog` (shadcn Dialog 기반)
  - [x] `nextjs-frontend/components/ui/dialog.tsx` 신규 생성 (아래 Dev Notes 코드 참조)

- [x] Task 3: definitions.ts 타입 확장 (AC: #2)
  - [x] `nextjs-frontend/lib/definitions.ts`에 `ComparisonEntry` 타입 추가 (name: string; monthly_revenue: number)
  - [x] `SimulationResultData`에 `comparison_data: ComparisonEntry[]` 필드 추가

- [x] Task 4: ComparisonBarChart 컴포넌트 (AC: #2)
  - [x] `nextjs-frontend/components/simulation/ComparisonBarChart.tsx` 신규 생성 (`"use client"`)
  - [ ] Recharts `ResponsiveContainer(100%, 220px)` + `BarChart` + `Bar` + `Cell`
  - [ ] 첫 번째 entry (isTarget=true, "분석 대상") → `#2563EB`, 나머지 → `#BFDBFE`
  - [ ] XAxis: 지점명(최대 6자), YAxis: 숨김, Tooltip: `{만원}` 포맷
  - [ ] Props: `data: ComparisonEntry[]`, `targetRevenue: number`

- [x] Task 5: ProposalModal 컴포넌트 (AC: #1, #3, #4)
  - [x] `nextjs-frontend/components/proposal/ProposalModal.tsx` 신규 생성 (`"use client"`)
  - [x] `Dialog` + `DialogContent` (shadcn dialog.tsx 사용), `max-w-[680px] w-[90vw]`
  - [x] `onInteractOutside={(e) => e.preventDefault()}` + `onEscapeKeyDown={(e) => e.preventDefault()}` (ESC/외부 클릭 차단)
  - [ ] 헤더: "🏠 다락 (Dalock) — 수익분석 제안서" + 오른쪽 X 버튼 (onClose)
  - [ ] 물건 정보 행: 주소, 면적(㎡), 임대료(만원), 건축물용도, EV충전 여부
  - [ ] 수치 도표: 예상 월매출 / 점유율 / 순수익 카드 (3열 grid)
  - [ ] ComparisonBarChart: `comparison_data`가 있을 때만 렌더링
  - [ ] VerdictBadge: verdict + percentile 표시
  - [ ] 하단: 출력 날짜(YYYY.MM.DD), "닫기" 버튼
  - [ ] Props: `open: boolean`, `onClose: () => void`, `result: SimulationResultData`, `input: SimulationInput`

- [x] Task 6: SimulationForm onResult 시그니처 변경 (AC: #1)
  - [x] `nextjs-frontend/components/simulation/SimulationForm.tsx` 수정
  - [x] `onResult?: (result: SimulationResultData, input: SimulationInput) => void`로 변경
  - [x] `import { type SimulationInput } from "@/lib/definitions"` 추가
  - [ ] 성공 핸들러: `onResult?.(res.result, parsed.data)` (address → parsed.data 전체)

- [x] Task 7: SimulationContainer 모달 상태 통합 (AC: #1, #3)
  - [x] `nextjs-frontend/components/simulation/SimulationContainer.tsx` 수정
  - [x] `submittedInput: SimulationInput | null` 상태 추가
  - [x] `proposalOpen: boolean` 상태 추가
  - [x] `handleResult(r, input)` — `setSubmittedInput(input)`, `setMapRequest(...)` 에 `input.address` 사용
  - [x] `SimulationResultCard`에 `onProposalClick={() => setProposalOpen(true)}` 전달
  - [x] `ProposalModal` 렌더링 (result && submittedInput 일 때)

- [x] Task 8: SimulationResultCard 버튼 placeholder 제거 (AC: #1)
  - [x] `nextjs-frontend/components/simulation/SimulationResultCard.tsx` 수정
  - [ ] "제안서 출력" 버튼에서 `title="제안서 출력 기능은 곧 추가됩니다"` 제거

- [x] Task 9: TypeScript 타입 검사
  - [x] `cd nextjs-frontend && npx tsc --noEmit` — 오류 없음

## Dev Notes

### 현재 상태 (Story 3-1 이후)

**SimulationResultCard.tsx 현재 상태:**
```tsx
type Props = {
  result: SimulationResultData;
  onProposalClick?: () => void;  // ← 이미 prop 정의됨
};
// 버튼: onClick={onProposalClick} title="제안서 출력 기능은 곧 추가됩니다" ← title만 제거
```

**SimulationForm.tsx 현재 onResult 시그니처:**
```tsx
// 현재:
type Props = {
  onResult?: (result: SimulationResultData, address: string) => void;
};
// 변경 후:
type Props = {
  onResult?: (result: SimulationResultData, input: SimulationInput) => void;
};
// 성공 핸들러:
onResult?.(res.result, parsed.data);  // parsed.data가 SimulationInput 타입
```

**SimulationContainer.tsx 현재 handleResult:**
```tsx
function handleResult(r: SimulationResultData, address: string) {
  setResult(r);
  setResultVersion((v) => v + 1);
  setMapRequest((prev) => ({ address, seq: (prev?.seq ?? 0) + 1 }));
}
// 변경 후:
function handleResult(r: SimulationResultData, input: SimulationInput) {
  setResult(r);
  setSubmittedInput(input);  // ← 추가
  setResultVersion((v) => v + 1);
  setMapRequest((prev) => ({ address: input.address, seq: (prev?.seq ?? 0) + 1 }));
}
```

### SimulationInput 타입

`lib/definitions.ts`에서 `z.infer<typeof simulationInputSchema>` — 다음 필드:
```typescript
export type SimulationInput = z.infer<typeof simulationInputSchema>;
// {
//   address: string;
//   areaSqm: number;
//   monthlyRent: number;
//   maintenanceFee?: number;
//   buildingUse?: "상업용" | "근린생활시설" | "창고" | "기타";
//   evCharging: boolean;
//   parkingSpots?: number;
// }
```

### 백엔드 comparison_data 구현 패턴

`fastapi_backend/app/schemas/simulation.py` 변경:
```python
class ComparisonEntry(BaseModel):
    name: str
    monthly_revenue: float

class SimulationResult(BaseModel):
    estimated_monthly_revenue: float
    occupancy_rate: float
    net_profit: float
    percentile: float
    verdict: Literal["추천", "검토필요", "비추천"]
    similar_branch_count: int
    fallback_used: bool = False
    comparison_data: list[ComparisonEntry] = []  # ← 추가
```

`fastapi_backend/app/engines/rule_based.py` — `predict()` 마지막 return 바로 전에:
```python
# comparison_data: effective 중 상위 5개 (avg_monthly_revenue 내림차순)
top5 = sorted(effective, key=lambda m: m.avg_monthly_revenue, reverse=True)[:5]
comparison_data = [
    ComparisonEntry(
        name=(m.branch_name[:12] + "…" if len(m.branch_name) > 12 else m.branch_name),
        monthly_revenue=round(m.avg_monthly_revenue, 2),
    )
    for m in top5
]

return SimulationResult(
    estimated_monthly_revenue=round(weighted_rev, 2),
    # ... 나머지 필드 ...
    comparison_data=comparison_data,
)
```

Import 추가: `from app.schemas.simulation import LocationConditions, SimulationResult, ComparisonEntry`

### Dialog UI 컴포넌트 (shadcn 패턴)

`nextjs-frontend/components/ui/dialog.tsx`:
```tsx
"use client";
import * as DialogPrimitive from "@radix-ui/react-dialog";
import { X } from "lucide-react";
import { cn } from "@/lib/utils";

const Dialog = DialogPrimitive.Root;
const DialogTrigger = DialogPrimitive.Trigger;
const DialogPortal = DialogPrimitive.Portal;
const DialogClose = DialogPrimitive.Close;

function DialogOverlay({ className, ...props }: React.ComponentPropsWithoutRef<typeof DialogPrimitive.Overlay>) {
  return (
    <DialogPrimitive.Overlay
      className={cn("fixed inset-0 z-50 bg-black/50 data-[state=open]:animate-in data-[state=closed]:animate-out data-[state=closed]:fade-out-0 data-[state=open]:fade-in-0", className)}
      {...props}
    />
  );
}

function DialogContent({
  className,
  children,
  ...props
}: React.ComponentPropsWithoutRef<typeof DialogPrimitive.Content>) {
  return (
    <DialogPortal>
      <DialogOverlay />
      <DialogPrimitive.Content
        className={cn(
          "fixed left-[50%] top-[50%] z-50 translate-x-[-50%] translate-y-[-50%] bg-white shadow-xl rounded-xl",
          "data-[state=open]:animate-in data-[state=closed]:animate-out",
          "data-[state=closed]:fade-out-0 data-[state=open]:fade-in-0",
          "data-[state=closed]:zoom-out-95 data-[state=open]:zoom-in-95",
          "data-[state=closed]:slide-out-to-left-1/2 data-[state=closed]:slide-out-to-top-[48%]",
          "data-[state=open]:slide-in-from-left-1/2 data-[state=open]:slide-in-from-top-[48%]",
          "duration-200",
          className
        )}
        {...props}
      >
        {children}
      </DialogPrimitive.Content>
    </DialogPortal>
  );
}

export { Dialog, DialogTrigger, DialogPortal, DialogOverlay, DialogClose, DialogContent };
```

**중요:** `DialogContent`에 `X` 버튼을 내장하지 않는다. ProposalModal에서 직접 구현.

### ComparisonBarChart 구현 패턴

```tsx
"use client";
import { BarChart, Bar, Cell, XAxis, Tooltip, ResponsiveContainer } from "recharts";
import { type ComparisonEntry } from "@/lib/definitions";

type ChartEntry = { name: string; monthly_revenue: number; isTarget: boolean };

type Props = {
  data: ComparisonEntry[];        // 유사 지점 데이터
  targetRevenue: number;          // 분석 대상 예상 월매출
};

export default function ComparisonBarChart({ data, targetRevenue }: Props) {
  const chartData: ChartEntry[] = [
    { name: "분석 대상", monthly_revenue: targetRevenue, isTarget: true },
    ...data.map((d) => ({ ...d, isTarget: false })),
  ];

  return (
    <ResponsiveContainer width="100%" height={220}>
      <BarChart data={chartData} margin={{ top: 8, right: 8, left: 8, bottom: 24 }}>
        <XAxis
          dataKey="name"
          tick={{ fontSize: 11, fill: "#64748B" }}
          tickLine={false}
          axisLine={false}
          interval={0}
          angle={-20}
          textAnchor="end"
        />
        <Tooltip
          formatter={(value: number) => [`${Math.round(value / 10_000).toLocaleString("ko-KR")}만원`, "예상 월매출"]}
          contentStyle={{ borderRadius: "8px", fontSize: "12px" }}
        />
        <Bar dataKey="monthly_revenue" radius={[4, 4, 0, 0]}>
          {chartData.map((entry, i) => (
            <Cell key={i} fill={entry.isTarget ? "#2563EB" : "#BFDBFE"} />
          ))}
        </Bar>
      </BarChart>
    </ResponsiveContainer>
  );
}
```

### ProposalModal 구현 패턴

```tsx
"use client";
import { Dialog, DialogContent } from "@/components/ui/dialog";
import { type SimulationResultData, type SimulationInput, type ComparisonEntry } from "@/lib/definitions";
import VerdictBadge from "@/components/simulation/VerdictBadge";
import ComparisonBarChart from "@/components/simulation/ComparisonBarChart";

type Props = {
  open: boolean;
  onClose: () => void;
  result: SimulationResultData;
  input: SimulationInput;
};

function formatManWon(v: number) { return `${Math.round(v / 10_000).toLocaleString("ko-KR")}만원`; }
function today() { const d = new Date(); return `${d.getFullYear()}.${String(d.getMonth()+1).padStart(2,"0")}.${String(d.getDate()).padStart(2,"0")}`; }

export default function ProposalModal({ open, onClose, result, input }: Props) {
  return (
    <Dialog open={open} onOpenChange={(v) => { if (!v) onClose(); }}>
      <DialogContent
        className="max-w-[680px] w-[90vw] max-h-[90vh] overflow-y-auto flex flex-col gap-0 p-0"
        onInteractOutside={(e) => e.preventDefault()}
        onEscapeKeyDown={(e) => e.preventDefault()}
      >
        {/* 헤더 */}
        <div className="flex items-center justify-between px-6 py-4 border-b border-dalock-border">
          <div>
            <p className="text-xs text-dalock-text2">🏠 다락 (Dalock)</p>
            <h2 className="text-lg font-semibold text-dalock-text1">수익분석 제안서</h2>
          </div>
          <button
            onClick={onClose}
            aria-label="닫기"
            className="p-1.5 rounded-md hover:bg-dalock-surface text-dalock-text2 hover:text-dalock-text1 transition-colors"
          >
            ✕
          </button>
        </div>

        {/* 본문 */}
        <div className="px-6 py-5 flex flex-col gap-5">
          {/* 물건 정보 */}
          <section>
            <h3 className="text-sm font-semibold text-dalock-text2 mb-2">📋 물건 정보</h3>
            <div className="bg-dalock-surface rounded-lg p-4 flex flex-wrap gap-x-6 gap-y-1 text-sm text-dalock-text1">
              <span><span className="text-dalock-text2">주소</span> {input.address}</span>
              <span><span className="text-dalock-text2">면적</span> {input.areaSqm}㎡</span>
              <span><span className="text-dalock-text2">임대료</span> {Math.round(input.monthlyRent / 10_000)}만원</span>
              {input.buildingUse && <span><span className="text-dalock-text2">용도</span> {input.buildingUse}</span>}
              <span><span className="text-dalock-text2">EV충전</span> {input.evCharging ? "가능" : "불가"}</span>
            </div>
          </section>

          {/* 수치 도표 */}
          <section>
            <h3 className="text-sm font-semibold text-dalock-text2 mb-2">📊 시뮬레이션 결과</h3>
            <div className="grid grid-cols-3 gap-3">
              {[
                { label: "예상 월매출", value: formatManWon(result.estimated_monthly_revenue) },
                { label: "예상 점유율", value: `${result.occupancy_rate.toFixed(1)}%` },
                { label: "예상 순수익", value: formatManWon(result.net_profit), danger: result.net_profit < 0 },
              ].map(({ label, value, danger }) => (
                <div key={label} className="bg-dalock-surface rounded-lg p-4 text-center">
                  <p className="text-xs text-dalock-text2 mb-1">{label}</p>
                  <p className={`text-xl font-bold ${danger ? "text-dalock-danger" : "text-dalock-text1"}`}>{value}</p>
                </div>
              ))}
            </div>
          </section>

          {/* 비교 차트 */}
          {result.comparison_data.length > 0 && (
            <section>
              <h3 className="text-sm font-semibold text-dalock-text2 mb-2">📈 브랜드 내 유사 지점 비교</h3>
              <ComparisonBarChart
                data={result.comparison_data}
                targetRevenue={result.estimated_monthly_revenue}
              />
              <p className="text-xs text-dalock-text2 mt-1 text-center">
                <span className="inline-block w-3 h-3 rounded-sm bg-[#2563EB] mr-1 align-middle" />분석 대상
                <span className="inline-block w-3 h-3 rounded-sm bg-[#BFDBFE] ml-3 mr-1 align-middle" />유사 지점
              </p>
            </section>
          )}

          {/* 판정 배지 */}
          <section className="flex items-center gap-3">
            <VerdictBadge verdict={result.verdict} percentile={result.percentile} />
            {result.similar_branch_count > 0 && (
              <span className="text-xs text-dalock-text2">유사 지점 {result.similar_branch_count}개 기준</span>
            )}
          </section>
        </div>

        {/* 하단 */}
        <div className="flex items-center justify-between px-6 py-4 border-t border-dalock-border">
          <span className="text-xs text-dalock-text2">{today()}</span>
          <button
            onClick={onClose}
            className="px-4 py-2 text-sm rounded-md border border-dalock-border text-dalock-text1 hover:bg-dalock-surface transition-colors"
          >
            닫기
          </button>
        </div>
      </DialogContent>
    </Dialog>
  );
}
```

### SimulationContainer 변경 요점

```tsx
// 추가할 import
import { type SimulationInput } from "@/lib/definitions";
import ProposalModal from "@/components/proposal/ProposalModal";

// 추가할 state
const [submittedInput, setSubmittedInput] = useState<SimulationInput | null>(null);
const [proposalOpen, setProposalOpen] = useState(false);

// handleResult 변경 (address → input)
function handleResult(r: SimulationResultData, input: SimulationInput) {
  setResult(r);
  setSubmittedInput(input);
  setResultVersion((v) => v + 1);
  setMapRequest((prev) => ({ address: input.address, seq: (prev?.seq ?? 0) + 1 }));
}

// SimulationResultCard에 onProposalClick 전달 (결과 카드 overlay 내)
<SimulationResultCard key={resultVersion} result={result} onProposalClick={() => setProposalOpen(true)} />

// 모달 렌더링 (SimulationContainer JSX 최하단)
{proposalOpen && result && submittedInput && (
  <ProposalModal
    open={proposalOpen}
    onClose={() => setProposalOpen(false)}
    result={result}
    input={submittedInput}
  />
)}
```

### SimulationForm 변경 요점

```tsx
// import 추가
import { simulationInputSchema, type SimulationResultData, type SimulationInput } from "@/lib/definitions";

// Props 타입 변경
type Props = {
  onResult?: (result: SimulationResultData, input: SimulationInput) => void;
};

// handleSubmit 성공 핸들러 변경
onResult?.(res.result, parsed.data);  // address만 → parsed.data 전체
```

### 라이브러리 호환성 주의

- **recharts**: `npm install recharts` — v2.x는 `@types/recharts` 불필요 (번들 타입 포함)
- **@radix-ui/react-dialog**: `npm install @radix-ui/react-dialog`
- **Recharts + Next.js**: SSR에서 `ResponsiveContainer`가 `width="100%"` 시 0px 렌더 가능 → 필요하면 `dynamic({ ssr: false })` 사용. 단 ProposalModal 자체가 Dialog 내부에 렌더링되므로 CSR에서 항상 호출됨 — SSR 문제 없음.
- **recharts Cell**: `<Cell key={i} fill={...} />` — 반드시 `key` prop 필요

### 접근성 (WCAG AA)

- Dialog: `aria-modal="true"` (Radix 자동 처리)
- X 버튼: `aria-label="닫기"` 필수
- 차트: `aria-label="유사 지점 월매출 비교 차트"` 를 `<section>` aria-label에 추가
- 모달 스크롤: `max-h-[90vh] overflow-y-auto` → 뷰포트 초과 시 스크롤 가능

### NFR 준수

- NFR-2: ComparisonBarChart는 클라이언트 전용, 백엔드가 comparison_data 전달 — API 키 노출 없음
- NFR-7: WCAG AA — 버튼 44px 이상, aria-label 추가

### 기존 파일 수정 시 주의사항

| 파일 | 주의 |
|------|------|
| `SimulationContainer.tsx` | `mapRequest` 생성 시 `address` 추출 위치: `input.address` |
| `SimulationForm.tsx` | `onResult` prop 타입 변경이 `SimulationContainer`에도 연쇄 영향 — 반드시 함께 수정 |
| `rule_based.py` | `ComparisonEntry` import 경로: `from app.schemas.simulation import ..., ComparisonEntry` |

### 파일 위치 정리

| 파일 | 상태 |
|------|------|
| `nextjs-frontend/components/ui/dialog.tsx` | 신규 |
| `nextjs-frontend/components/simulation/ComparisonBarChart.tsx` | 신규 |
| `nextjs-frontend/components/proposal/ProposalModal.tsx` | 신규 |
| `fastapi_backend/app/schemas/simulation.py` | 수정 |
| `fastapi_backend/app/engines/rule_based.py` | 수정 |
| `nextjs-frontend/lib/definitions.ts` | 수정 |
| `nextjs-frontend/components/simulation/SimulationForm.tsx` | 수정 |
| `nextjs-frontend/components/simulation/SimulationContainer.tsx` | 수정 |
| `nextjs-frontend/components/simulation/SimulationResultCard.tsx` | 수정 |

## Dev Agent Record

### Implementation Plan

1. 백엔드 schema + rule_based 수정 → comparison_data 포함
2. 기존 시뮬레이션 테스트 보강
3. 프론트엔드 패키지 설치 (recharts, @radix-ui/react-dialog)
4. dialog.tsx 생성 (UI 컴포넌트)
5. definitions.ts 타입 확장
6. ComparisonBarChart.tsx 신규 생성
7. ProposalModal.tsx 신규 생성
8. SimulationForm.tsx 수정 (onResult 시그니처)
9. SimulationContainer.tsx 수정 (modal 상태 + handleResult 수정)
10. SimulationResultCard.tsx 수정 (placeholder title 제거)
11. npx tsc --noEmit

### Debug Log

_(구현 중 추가)_

### Completion Notes

- ComparisonEntry 스키마 추가, SimulationResult.comparison_data (상위 5개 유사지점, 이름 12자 트런케이트)
- RuleBasedEngine.predict(): effective 목록 avg_monthly_revenue 내림차순 top5 → comparison_data
- SimulationForm.onResult 시그니처: address → SimulationInput 전체 전달
- SimulationContainer: submittedInput + proposalOpen 상태, handleResult 수정, ProposalModal 렌더링
- ProposalModal: @radix-ui/react-dialog 기반, ESC/외부클릭 차단, X버튼 전용 닫기, 물건정보/수치도표/비교차트/판정배지
- ComparisonBarChart: Recharts ResponsiveContainer + Bar + Cell (#2563EB 분석대상/#BFDBFE 유사지점)
- dialog.tsx: shadcn 패턴 UI 컴포넌트 신규 생성
- simulation-action.ts: comparison_data 배열 정규화 guard 추가
- test_engines.py: db 파라미터 누락 기존 실패 수정, comparison_data assertion 추가
- 24 backend tests 통과, tsc --noEmit clean

## File List

**신규 생성:**
- `nextjs-frontend/components/ui/dialog.tsx`
- `nextjs-frontend/components/simulation/ComparisonBarChart.tsx`
- `nextjs-frontend/components/proposal/ProposalModal.tsx`

**수정:**
- `fastapi_backend/app/schemas/simulation.py`
- `fastapi_backend/app/engines/rule_based.py`
- `nextjs-frontend/lib/definitions.ts`
- `nextjs-frontend/components/simulation/SimulationForm.tsx`
- `nextjs-frontend/components/simulation/SimulationContainer.tsx`
- `nextjs-frontend/components/simulation/SimulationResultCard.tsx`

## Change Log

- 2026-06-12: Story 3-2 CS 생성. ProposalModal(680px, X버튼 전용 닫기) + ComparisonBarChart(Recharts, #2563EB/#BFDBFE) + 백엔드 comparison_data 확장. SimulationInput 전체를 onResult에 전달하는 방식으로 데이터 흐름 개선.
- 2026-06-12: DS 완료 — 9개 태스크 구현, 24 테스트 통과, tsc clean.
