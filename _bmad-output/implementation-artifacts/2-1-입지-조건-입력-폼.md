# Story 2.1: 입지 조건 입력 폼

Status: done

## Story

As a 관리자,
I want 검토 중인 부동산의 입지 조건을 입력하고 싶다,
So that 시뮬레이션 실행 버튼을 클릭하여 수익 분석을 시작할 수 있다.

## Acceptance Criteria

1. **Given** `/dashboard/simulation` 페이지 접근 **When** 페이지 렌더링 **Then** 주소(텍스트), 면적(숫자), 월 임대료(숫자), 관리비(숫자), 건축물용도(셀렉트), EV충전(토글), 주차대수(숫자) 입력 필드가 표시됨

2. **Given** 필수 항목(주소, 면적, 임대료) 중 하나 이상 미입력 **When** 폼 상태 확인 **Then** "시뮬레이션 실행" 버튼이 비활성(disabled) 상태임

3. **Given** 모든 필수 항목 입력 완료 **When** "시뮬레이션 실행" 버튼 클릭 **Then** 버튼이 비활성화되고 "분석 중..." 인라인 스피너가 표시됨 (UX-DR14)

4. **Given** 잘못된 데이터 형식(예: 면적에 문자 입력) **When** 해당 필드에서 포커스 이탈(blur) **Then** 필드 하단에 빨간 오류 메시지가 표시되고 테두리가 빨간색으로 강조됨

## Tasks / Subtasks

- [x] Task 1: simulationInputSchema 정의 (AC: #2, #4)
  - [ ] `nextjs-frontend/lib/definitions.ts`에 `simulationInputSchema` 추가
  - [ ] 필수: `address: z.string().min(1, "주소를 입력하세요")`
  - [ ] 필수: `areaSqm: z.coerce.number({ invalid_type_error: "숫자를 입력하세요" }).positive("0보다 커야 합니다")`
  - [ ] 필수: `monthlyRent: z.coerce.number({ invalid_type_error: "숫자를 입력하세요" }).positive("0보다 커야 합니다")`
  - [ ] 선택: `maintenanceFee: z.coerce.number({ invalid_type_error: "숫자를 입력하세요" }).nonnegative().optional()`
  - [ ] 선택: `buildingUse: z.enum(["상업용", "근린생활시설", "창고", "기타"]).optional()`
  - [ ] 선택: `evCharging: z.boolean().optional().default(false)`
  - [ ] 선택: `parkingSpots: z.coerce.number({ invalid_type_error: "숫자를 입력하세요" }).int().nonnegative().optional()`
  - [ ] `export type SimulationInput = z.infer<typeof simulationInputSchema>`

- [x] Task 2: 시뮬레이션 서버 액션 stub 생성 (AC: #3)
  - [ ] `nextjs-frontend/components/actions/simulation-action.ts` 신규 생성 (`"use server"`)
  - [ ] `runSimulation(input: SimulationInput): Promise<void>` stub — Story 2-2에서 실제 구현 예정
  - [ ] 현재는 `await new Promise(r => setTimeout(r, 1500))` (1.5초 딜레이)로 로딩 상태 시연
  - [ ] 반환 타입: `{ error?: string }` (Story 2-2에서 `SimulationResult`로 확장)

- [x] Task 3: SimulationForm 클라이언트 컴포넌트 생성 (AC: #1, #2, #3, #4)
  - [ ] `nextjs-frontend/components/simulation/SimulationForm.tsx` 신규 생성 (`"use client"`)
  - [ ] `useState`로 폼 값 관리: `{ address, areaSqm, monthlyRent, maintenanceFee, buildingUse, evCharging, parkingSpots }`
  - [ ] `useState<Record<string, string>>`로 필드별 오류 관리
  - [ ] **필수 항목 비활성 로직**: `address === "" || areaSqm === "" || monthlyRent === ""` 이면 submit 버튼 `disabled`
  - [ ] **onBlur 검증**: 각 필드 blur 시 해당 필드만 `simulationInputSchema.pick({fieldName}).safeParse()`로 검증 → 오류 시 errors state 업데이트
  - [ ] **오류 표시**: 오류 있는 Input에 `border-dalock-danger` 클래스, 하단에 `<p className="text-dalock-danger text-xs mt-1">{error}</p>`
  - [ ] **submit 핸들러**: `useTransition`으로 isPending 상태 관리, `startTransition(() => runSimulation(input))`
  - [ ] **loading UI**: isPending 시 버튼 `disabled` + 버튼 텍스트 "분석 중..." + 인라인 SVG 스피너
  - [ ] **건축물용도 셀렉트**: shadcn `Select` 컴포넌트 사용
  - [ ] **EV충전**: `<input type="checkbox">` 스타일드 토글
  - [ ] **주차대수**: `type="number"` input, `min="0"` 속성

- [x] Task 4: simulation/page.tsx에 SimulationForm 통합 (AC: #1)
  - [ ] `nextjs-frontend/app/dashboard/simulation/page.tsx` 수정
  - [ ] 좌측 패널에 `<SimulationForm />` 렌더링
  - [ ] placeholder 텍스트 제거

- [x] Task 5: TypeScript 타입 체크 (AC: all)
  - [ ] `cd nextjs-frontend && npx tsc --noEmit` 실행 — 오류 없음 확인

## Dev Notes

### 아키텍처 결정: Client Component 선택 이유

이 폼은 다음을 요구하므로 `"use client"` 선택:
- blur 시 실시간 인라인 검증 (서버 왕복 없이 즉시)
- 필수 항목 채워짐 여부에 따른 버튼 활성/비활성 동적 제어
- `useTransition`으로 isPending 추적

Server Action(`runSimulation`)은 `useTransition`의 `startTransition`으로 호출 — `useActionState` 패턴은 Story 2-3에서 결과 상태 관리 시 적용.

### simulationInputSchema

```ts
export const simulationInputSchema = z.object({
  address: z.string().min(1, "주소를 입력하세요"),
  areaSqm: z.coerce
    .number({ invalid_type_error: "숫자를 입력하세요" })
    .positive("0보다 커야 합니다"),
  monthlyRent: z.coerce
    .number({ invalid_type_error: "숫자를 입력하세요" })
    .positive("0보다 커야 합니다"),
  maintenanceFee: z.coerce
    .number({ invalid_type_error: "숫자를 입력하세요" })
    .nonnegative()
    .optional(),
  buildingUse: z.enum(["상업용", "근린생활시설", "창고", "기타"]).optional(),
  evCharging: z.boolean().optional().default(false),
  parkingSpots: z.coerce
    .number({ invalid_type_error: "숫자를 입력하세요" })
    .int()
    .nonnegative()
    .optional(),
});

export type SimulationInput = z.infer<typeof simulationInputSchema>;
```

### SimulationForm 컴포넌트 구조

```tsx
"use client";
import { useState, useTransition } from "react";
import { simulationInputSchema, type SimulationInput } from "@/lib/definitions";
import { runSimulation } from "@/components/actions/simulation-action";
import { Input } from "@/components/ui/input";
import { Label } from "@/components/ui/label";
import { Select, SelectContent, SelectItem, SelectTrigger, SelectValue } from "@/components/ui/select";

export default function SimulationForm() {
  const [isPending, startTransition] = useTransition();
  const [values, setValues] = useState({ address: "", areaSqm: "", monthlyRent: "", maintenanceFee: "", buildingUse: "", evCharging: false, parkingSpots: "" });
  const [errors, setErrors] = useState<Record<string, string>>({});

  const isSubmitDisabled = !values.address || !values.areaSqm || !values.monthlyRent || isPending;

  function validateField(name: string, value: string | boolean) {
    const partialSchema = simulationInputSchema.pick({ [name]: true } as any);
    const result = partialSchema.safeParse({ [name]: value });
    if (!result.success) {
      setErrors(prev => ({ ...prev, [name]: result.error.errors[0].message }));
    } else {
      setErrors(prev => { const next = { ...prev }; delete next[name]; return next; });
    }
  }

  function handleSubmit(e: React.FormEvent) {
    e.preventDefault();
    const parsed = simulationInputSchema.safeParse({
      ...values,
      areaSqm: values.areaSqm,
      monthlyRent: values.monthlyRent,
    });
    if (!parsed.success) return;
    startTransition(() => {
      runSimulation(parsed.data);
    });
  }

  return (
    <form onSubmit={handleSubmit} className="flex flex-col gap-4">
      {/* 각 필드 */}
      ...
      <button type="submit" disabled={isSubmitDisabled} className="...">
        {isPending ? (
          <><span className="animate-spin ...">⏳</span> 분석 중...</>
        ) : "시뮬레이션 실행"}
      </button>
    </form>
  );
}
```

### 인라인 스피너 (UX-DR14)

SVG 스피너 (tailwind animate-spin):
```tsx
<svg className="animate-spin h-4 w-4 mr-2" viewBox="0 0 24 24">
  <circle className="opacity-25" cx="12" cy="12" r="10" stroke="currentColor" strokeWidth="4" fill="none" />
  <path className="opacity-75" fill="currentColor" d="M4 12a8 8 0 018-8V0C5.373 0 0 5.373 0 12h4z" />
</svg>
```

### onBlur 검증 패턴

숫자 필드에 문자 입력 시 red border 조건:
- Input className: `errors.areaSqm ? "border-dalock-danger focus-visible:ring-dalock-danger" : ""`
- blur 시 `validateField("areaSqm", values.areaSqm)` 호출

### 건축물용도 옵션

shadcn Select 사용:
```tsx
<Select value={values.buildingUse} onValueChange={(v) => setValues(prev => ({...prev, buildingUse: v}))}>
  <SelectTrigger><SelectValue placeholder="선택하세요" /></SelectTrigger>
  <SelectContent>
    <SelectItem value="상업용">상업용</SelectItem>
    <SelectItem value="근린생활시설">근린생활시설</SelectItem>
    <SelectItem value="창고">창고</SelectItem>
    <SelectItem value="기타">기타</SelectItem>
  </SelectContent>
</Select>
```

### EV충전 토글

```tsx
<div className="flex items-center gap-2">
  <input
    type="checkbox"
    id="evCharging"
    checked={values.evCharging}
    onChange={(e) => setValues(prev => ({...prev, evCharging: e.target.checked}))}
    className="w-4 h-4 accent-dalock-primary"
  />
  <Label htmlFor="evCharging">EV충전 가능</Label>
</div>
```

### simulation-action stub

```ts
"use server";
import { SimulationInput } from "@/lib/definitions";

export async function runSimulation(_input: SimulationInput): Promise<{ error?: string }> {
  // Story 2-2에서 POST /simulation/run API 연결 예정
  await new Promise((r) => setTimeout(r, 1500));
  return {};
}
```

### Story 1-5 학습 사항 반영

- `"use client"` 컴포넌트: TypeScript strict 모드에서 `simulationInputSchema.pick({ [name]: true } as any)` 대신 필드별 개별 검증 함수로 타입 안전성 확보
- 에러 메시지: `text-dalock-danger` (기존 `text-red-500` 대신 브랜드 토큰 사용)
- 버튼 Primary 스타일: `bg-dalock-primary text-white hover:bg-blue-700 disabled:opacity-50` — Story 1-5 AC2 defer 항목 해결

### 기존 파일 수정 대상

| 파일 | 변경 유형 |
|------|----------|
| `nextjs-frontend/lib/definitions.ts` | 수정 — simulationInputSchema 추가 |
| `nextjs-frontend/app/dashboard/simulation/page.tsx` | 수정 — SimulationForm 통합 |

| 파일 | 변경 유형 |
|------|----------|
| `nextjs-frontend/components/simulation/SimulationForm.tsx` | 신규 |
| `nextjs-frontend/components/actions/simulation-action.ts` | 신규 (stub) |

## Dev Agent Record

### Implementation Plan

1. `lib/definitions.ts`에 simulationInputSchema + SimulationInput 타입 추가
2. `simulation-action.ts` Server Action stub (1.5초 딜레이 시뮬레이션)
3. `SimulationForm.tsx` 클라이언트 컴포넌트: useState 폼 값, onBlur 검증, useTransition 로딩
4. `simulation/page.tsx` 좌측 패널에 SimulationForm 통합

### Debug Log

- `simulationInputSchema.pick({ field: true })` 패턴: 필드별 개별 검증에서 각 케이스를 Record에 직접 매핑하여 타입 안전성 확보
- `z.coerce.number()`: 빈 문자열 입력 시 `NaN`으로 변환 → positive() 검증으로 적절히 처리됨
- `type="text" inputMode="decimal"`: 숫자 필드에 type="number" 대신 text를 사용하여 onBlur에서 문자 입력 감지 가능
- tsc --noEmit: 오류 없음

### Completion Notes

- 7개 입력 필드 모두 구현 (AC1 ✅)
- 필수 3개 항목 중 하나라도 빈 값이면 버튼 disabled (AC2 ✅)
- useTransition.isPending으로 버튼 비활성 + SVG 스피너 + "분석 중..." 텍스트 (AC3 ✅)
- onBlur에서 필드별 Zod 검증, 오류 시 border-dalock-danger + 빨간 텍스트 (AC4 ✅)
- Primary 버튼 bg-dalock-primary 사용 — Story 1-5 AC2 defer 항목 해결

## File List

**신규 생성:**
- `nextjs-frontend/components/simulation/SimulationForm.tsx`
- `nextjs-frontend/components/actions/simulation-action.ts`

**수정:**
- `nextjs-frontend/lib/definitions.ts` — simulationInputSchema, SimulationInput 추가
- `nextjs-frontend/app/dashboard/simulation/page.tsx` — SimulationForm 통합

## Change Log

- 2026-06-12: Story 2-1 CS 생성. 입지 조건 입력 폼 7개 필드, 필수 항목 비활성 로직, blur 검증, 로딩 상태 스코프 정의.
- 2026-06-12: Story 2-1 구현 완료. SimulationForm 7개 필드, useTransition 로딩 상태, Zod blur 검증. TypeScript 오류 없음.
- 2026-06-12: 코드 리뷰 5건 패치. isSubmitDisabled trim+numeric 강화, submit 실패 시 Zod errors→state 표시, serverError state 추가, onChange 즉시 재검증.
